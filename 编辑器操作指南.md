# 编辑器操作指南

> 本文档依据当前代码（`v1/frontend`）整理，适用于「多媒体内容管理展示系统」的**内容编辑器**。
> 系统由三部分组成：管理端（Vue3 网页）、服务端（FastAPI）、播放端（Electron/Windows 等）。编辑器属于管理端。

---

## 一、登录与进入编辑器

1. 浏览器打开管理端地址，进入登录页。
2. 输入用户名 / 密码登录。**默认超级管理员：`admin` / `admin123`**（首次启动由服务端自动创建）。
3. 顶部/侧边菜单进入 **「内容管理」**。
4. 在内容管理页可以：
   - **新建节目**：选择「展项 → 场景 → 设备」，填写节目名称，确定。
   - 对已有节目点 **「编辑」**，进入编辑器（路由 `/editor/{节目ID}`）。
   - 其它操作：发布、复制到设备、版本、分发、删除。
   - **一键发布场景**：选展项、场景、填变更说明，一次发布该场景下所有节目。

> 进入编辑器前需先有：展项、场景、设备，且设备已绑定到场景。

---

## 二、编辑器界面布局

```
┌───────────────────────────────────────────────────────────────┐
│ 顶部工具栏                                                      │
├────────────┬────────────────────────────────────┬─────────────┤
│ 左侧面板    │                                    │ 右侧面板     │
│ · 页面      │            画布（Canvas）           │ · 属性       │
│ · 素材      │                                    │ · 图层       │
│ · 装饰      │                                    │ · 动画       │
│            │                                    │ · 热区       │
├────────────┴────────────────────────────────────┴─────────────┤
│ 底部状态栏：缩放 / 适应 / 100% / 坐标 / 画布尺寸                │
└───────────────────────────────────────────────────────────────┘
```

**顶部工具栏**（从左到右）：
- 添加：**文字**、**容器**、**按钮**
- **功能控件**（下拉）：上一页、下一页、首页、末页、播放、暂停、停止（一键生成带图标/文字/动作的按钮）
- **分组** / **解散分组**
- **撤销** / **重做**
- **保存** / **预览** / **导出**
- 最右侧显示当前节目名称

**底部状态栏**：缩放滑块（10%~300%）、适应、100%、当前选中元素坐标、画布设计尺寸。

---

## 三、页面管理（左侧「页面」标签）

- **添加页面**：点「+ 添加」。默认名称“页面N”。
- **删除页面**：页面右上角 ✕（至少保留一页）。
- 每个页面卡片显示：页面名称、**层数**、以及页面设置项：

| 设置项 | 说明 |
|---|---|
| 时长(秒) | 该页自动播放停留时间（默认 10 秒） |
| 播放模式 | 顺序播放 / 循环播放 / 手动切换 |
| 过渡效果 | 淡入淡出 / 滑动 / 缩放 / 无 |
| 背景设置 | 见下 |

**背景设置**：
- 类型：无 / 纯色 / 渐变 / 图片 / 视频
- 纯色：选颜色
- 渐变：起色、止色、角度
- 图片/视频：填素材 Hash（或从属性面板“设为背景画面”）
- 通用：不透明度、亮度、模糊

> 背景永远位于所有图层最底层，不出现在图层列表，不可被选中。

---

## 四、素材库（左侧「素材」标签）

- **上传素材**：支持图片、视频。
- **批量导入**：一次选多张图片/视频，可勾选“去除日期前缀”，逐条预览/修改字幕后导入。导入时图片会自动合并为一个多图元素（带字幕），其它文件按网格排布到画布。
- **序列帧**：选择一个**文件夹**上传，系统按文件名排序生成帧列表。
- **搜索**：按文件名搜索。
- **类型筛选**：全部 / 图片 / 视频 / 序列帧。
- 每个素材显示缩略图、名称、**引用次数**；右上角 ✕ 删除。

**拖拽用法**：
- 把素材拖到画布空白处 → 新建元素。
- 把素材拖到**画布上已有的同类元素**上 → **追加到该元素的列表**（图片列表 / 视频列表），不新建元素。
- 序列帧文件夹拖到已有序列帧元素上 → 追加为一段。

---

## 五、装饰库（左侧「装饰」标签）

内置矢量装饰，分三类：
- **画框角花**：直角边框、圆角边框、四角角花、顶部标题栏等
- **分割线导航**：分割线、菱形夹线、左右箭头、科技斜条等
- **几何点缀光效**：五角星、星光、圆环、菱形、流光斜条等

点击即可添加到画布中央，或拖到画布指定位置。

---

## 六、添加与编辑元素

### 支持的 8 种元素
文字、图片、视频、序列帧、形状、按钮、装饰、容器。

### 添加方式
- 工具栏：文字 / 容器 / 按钮 / 功能控件
- 素材库拖拽：图片 / 视频 / 序列帧
- 装饰库：装饰

### 画布操作
| 操作 | 方式 |
|---|---|
| 选中 | 单击元素 |
| 多选 | Ctrl + 单击 |
| 取消选择 | 点画布空白处 |
| 移动 | 按住拖动 |
| 缩放 | 拖动四周 8 个控制点 |
| 旋转 | 拖动顶部旋转点 |
| 删除 | 选中后按 Delete |
| 画布缩放 | Ctrl + 滚轮 |
| 画布平移 | 鼠标中键拖动，或 Alt + 拖动 |

---

## 七、属性面板（右侧「属性」标签，按元素类型变化）

**通用**
- 位置与尺寸：X、Y、宽、高
- 变换：旋转、不透明度、圆角
- 图片/形状：发光（颜色+强度）、图片边框（颜色+宽度）
- 顶部按钮：删除元素、设为背景画面、清除背景

**文字**
- 字体、字号、颜色、对齐（左/中/右）、粗体
- 自动适应文字 / 适应文字
- 描边、阴影
- 出现效果（打字机、淡入等）及速度
- 内容（多行文本）

**图片**
- 图片列表：每项显示序号+文件名，可“背景”、删除；每项可填**字幕**并设置字幕 X/Y
- 列表切换按钮：一键生成“上一张/下一张”按钮
- 循环模式：自动+手动 / 仅自动 / 仅手动
- 手动后恢复自动(秒)、切换间隔(秒)、切换动画（渐入渐出/直接切换）
- 填充方式：覆盖/包含/填充/无
- 亮度、对比度

**字幕**（图片专属）
- 字体、字号、颜色、对齐、粗体、位置（顶部/底部）
- 字宽、边距、阴影强度、出现效果及速度
- 半透明背景条：启用、颜色、透明度、内边距
- 文字描边：启用、颜色、粗细
- 渐变遮罩：启用、位置（自动/顶部/底部/上下）

**视频**
- 视频列表（可多项，可“背景”、删除）
- 列表切换按钮（上一张/下一张）
- 循环模式、手动后恢复自动(秒)、切换间隔(秒)
- 循环、自动播放、静音、音量

**形状**
- 类型：矩形 / 圆形 / 线条 / 箭头
- 填充色

**按钮**
- 一键样式：10 套预设（经典蓝、科技渐变、霓虹发光、金色描边、玻璃拟态等）
- 图标：图标选择、图标颜色、图标尺寸
- 文字：内容、颜色、字号、图标与文字排列（左右/上下）
- 外观：背景形状（圆形/胶囊/圆角矩形/矩形/无）、圆角、填充（纯色或渐变）
- 描边与发光

**装饰**
- 样式：画框/分割/点缀，点选具体装饰
- 填充、描边色、线宽、发光

**序列帧**
- 段列表：每段可设置 帧率、循环次数（含 ∞）、方向（正向/反向/往复）、镜像、框内偏移X、位移（静止/移动 + 终点 + 移动时长）
- 帧内对齐比对：冻结该段首帧并叠加上一段末帧，用 ±0.1/±1/±10 微调对齐
- 默认帧率（兜底）、全局方向（兜底）、自动播放、整体循环
- 拖拽控制（仅单段且无位移时）：启用、灵敏度

---

## 八、图层面板（右侧「图层」标签）

- 列表**倒序显示**（最上层在最上面）。
- 每条图层：拖拽手柄、锁定图标、名称、显示/隐藏眼睛、更多菜单。
- 更多菜单：上移、下移、置顶、置底、复制、删除。
- 支持拖拽排序；Ctrl 点击可多选。
- **分组**：
  - 选中 2 个及以上普通元素 → 工具栏「分组」，生成一个容器把它们归为一组。
  - 拖动容器会带动组内成员一起移动；成员仍可单独选中、编辑。
  - 选中容器后点「解散分组」还原。
  - 目前**不支持组里套组**。

---

## 九、动画面板（右侧「动画」标签）

选中元素后：
- **添加动画**，可选：淡入、淡出、滑入、向上滑入、向下滑出、放大、缩小、旋转、移动。
- 每个动画可设置：**时长(秒)**、**延迟(秒)**、**方向**（入场/出场/强调）。
- 「移动」动画额外设置：起始位置、目标位置。
- 可删除已添加的动画。

---

## 十、热区面板（右侧「热区」标签）

给元素配置“点击后做什么”。

- **启用热区**（开关）
- **触发方式**：点击
- **动作**：
  切换页面、下一页、上一页、首页、末页、
  播放(全局)、暂停(全局)、停止(全局)、
  切换场景、切换节目、
  播放视频、暂停视频、
  上一张(媒体列表)、下一张(媒体列表)、
  发送TCP指令、无
- **目标**：
  - 切换页面/节目/场景：填目标 ID
  - 上一张/下一张：选择要控制的图片/视频元素
- **发送TCP指令**：TCP 目标主机、端口、编码（文本/HEX）、发送内容、结尾符（无/CRLF/LF）
- **作用范围**：仅本设备 / 整个场景 / 指定设备（可多选目标设备）
- **光标**：指针 / 手型
- **高亮**：开关

---

## 十一、预览

- 顶部「预览」按钮打开预览弹窗，从当前页开始播放。
- 支持：**全屏**（进入/退出）、上一页/下一页、页码指示（3 秒后自动隐藏）。
- 预览按各页面设定的播放模式、过渡效果、时长执行。
- 热区可点击；跨设备指令在预览中只打印到控制台（由播放器端执行）。

---

## 十二、保存与导出发布

- **保存**：工具栏「保存」或 **Ctrl + S**。保存到服务端（不通知设备）。
- **导出**：工具栏「导出」→ 填写**变更说明**（必填）→ 确认导出。
  - 导出会把当前配置发布为一个版本，并通知在线设备同步；离线设备下次联网自动更新。
  - 变更说明会写入版本历史，便于回滚定位。
- 离开编辑器时若有未保存内容，会提示“保存并离开 / 不保存直接离开”。

---

## 十三、快捷键一览

| 快捷键 | 功能 |
|---|---|
| Delete | 删除选中元素 |
| Ctrl + Z | 撤销 |
| Ctrl + Y | 重做 |
| Ctrl + S | 保存 |
| Ctrl + C | 复制选中元素 |
| Ctrl + V | 粘贴元素 |
| Ctrl + 滚轮 | 缩放画布 |
| 中键拖动 / Alt + 拖动 | 平移画布 |

> 在输入框/文本域中编辑时，Ctrl+C / Ctrl+V / Delete 交给系统原生处理。

---

## 十四、常见问题与注意

1. **图片/视频“列表”**：拖素材到已有同类元素上是**追加**到列表，不是新建；单张素材也是长度为 1 的列表。
2. **序列帧总时长**：多段位移时，页面自动切换时长要大于序列帧总时长，否则会被截断。
3. **背景**：设为背景后该素材会从图层中移除，只保留在页面背景；可用「清除背景」恢复。
4. **分组**：容器只用于编组与整体移动，播放端不把分组容器作为整体渲染；组内成员各自正常显示。
5. **热区跨设备**：目前“整个场景/指定设备”的指令通过服务器转发，需要设备在线。
6. **撤销/重做**：以整页快照方式记录，频繁操作会占用内存。
7. **画布缩放不影响数据**：元素坐标始终以设计分辨率（如 1920×1080）为准。
